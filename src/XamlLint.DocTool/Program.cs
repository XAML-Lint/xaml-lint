namespace XamlLint.DocTool;

internal static class Program
{
    public static int Main(string[] args)
    {
        System.Console.WriteLine("xaml-lint DocTool — stub. Wired up in M1.");
        System.Console.WriteLine($"args: [{string.Join(", ", args)}]");
        return 0;
    }
}
